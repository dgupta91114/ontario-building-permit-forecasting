# Reproducibility Checklist

Before making the repository public, confirm each item.

- [x] Public GitHub URL recorded: <https://github.com/dgupta91114/ontario-building-permit-forecasting>.
- [ ] Run `python scripts/00_inspect_sources.py` and review every selected source label.
- [ ] Freeze the source date range in `config/config.yaml`.
- [ ] Retain `data/raw/download_manifest.json` with source URLs, timestamps, and SHA-256 hashes.
- [ ] Commit `data/processed/modeling_dataset.csv` after verifying redistribution size and source attribution.
- [ ] Commit `data/processed/feature_audit.json` and `data/interim/cleaning_audit.json`.
- [ ] Run `pytest` successfully.
- [ ] Run `python run_pipeline.py` in a clean environment.
- [ ] Run `python scripts/08_reproducibility_check.py`.
- [ ] Confirm that no synthetic files under `data/demo/` were used for the paper.
- [ ] Confirm that every table and figure used in the paper is generated by a documented script.
- [ ] Record package versions with `pip freeze > environment-lock.txt` after final analysis.
- [ ] Review results for leakage, impossible dates, duplicate CMA-month rows, and missing targets.
- [ ] Report negative results and benchmark retention honestly.
- [ ] State that building permits represent construction intentions, not completed construction.
