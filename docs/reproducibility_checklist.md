# Reproducibility Checklist

Before making the repository public, confirm each item.

- [x] Public GitHub URL recorded: <https://github.com/dgupta91114/ontario-building-permit-forecasting>.
- [x] Run `python scripts/00_inspect_sources.py` and review every selected source label.
- [x] Freeze the source date range in `config/config.yaml`.
- [x] Retain `data/raw/download_manifest.json` with source URLs, timestamps, and SHA-256 hashes.
- [ ] Commit `data/processed/modeling_dataset.csv` after verifying redistribution size and source attribution.
- [x] Generate `data/processed/feature_audit.json` and `data/interim/cleaning_audit.json`.
- [x] Run `pytest` successfully.
- [x] Run `python run_pipeline.py --skip-download` in the clean Python 3.11 environment using frozen sources.
- [x] Run `python scripts/08_reproducibility_check.py`.
- [x] Confirm that no synthetic files under `data/demo/` were used for the paper.
- [x] Confirm that every table and figure used in the paper is generated by a documented script.
- [x] Record package versions in `environment-lock.txt` after final analysis.
- [x] Review results for leakage, impossible dates, duplicate CMA-month rows, and missing targets.
- [x] Report negative results and benchmark retention honestly.
- [x] State that building permits represent construction intentions, not completed construction.
